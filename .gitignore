# Local build output
/bin/
/chat2work-mcp
/chat2work-mcp.exe
*.test
*.out
coverage.*

# Local runtime data, audit logs, and scratch files
/data/
*.log

# Local configuration. Keep config.example.yaml committed as the template;
# every real config holds secrets and must never be committed.
/config.yaml
/config.*.yaml
!/config.example.yaml

# Environment files
.env
.env.*

# Editor and OS artifacts
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/
*.swp

# Local tooling artifacts
.omo/
.codegraph/
.opencode/
